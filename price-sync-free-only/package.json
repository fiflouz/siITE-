{
  "name": "price-sync-free-only",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "refresh": "node scripts/refresh-prices.mjs",
    "pw:install": "npx playwright install --with-deps chromium"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}